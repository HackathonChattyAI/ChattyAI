pre-commit:
  commands:
    lint:
      glob: '*.{ts,tsx}'
      run: npx eslint src/**/*.{js,ts,tsx} {staged_files}
    prettier:
      glob: "*.{ts,tsx}"
      run: npx prettier src/**/*.{js,ts,tsx} -w {staged_files}
      stage_fixed: true
